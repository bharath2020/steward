name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  isolated-macos-setup:
    name: Fresh macOS setup from GitHub
    runs-on: macos-14
    timeout-minutes: 20
    env:
      STEWARD_ISOLATED_SETUP_TEST: '1'
      HOMEBREW_NO_AUTO_UPDATE: '1'
    steps:
      - name: Record isolated host prerequisites
        run: |
          sw_vers
          command -v node || true
          command -v brew || true
          if command -v temporal; then
            echo "Expected a runner without Temporal so setup exercises its installation."
            exit 1
          fi
      - name: Download and install without checkout or Git credentials
        env:
          STEWARD_INSTALL_DIR: ${{ runner.temp }}/Steward
          STEWARD_REF: ${{ github.sha }}
        run: |
          test ! -e "$STEWARD_INSTALL_DIR"
          curl -fsSL "https://raw.githubusercontent.com/bharath2020/steward/$GITHUB_SHA/install.sh" | bash -s -- --example file --no-open
          test ! -e "$STEWARD_INSTALL_DIR/.git"
          test "$(cat "$STEWARD_INSTALL_DIR/.steward-install")" = "$GITHUB_SHA"
      - name: Verify Temporal completion, committed output, and repeat setup
        working-directory: ${{ runner.temp }}/Steward
        run: node --import tsx scripts/verify-installed-setup.cjs
      - name: Repeat archive installer without overwriting saved data
        env:
          STEWARD_INSTALL_DIR: ${{ runner.temp }}/Steward
        run: |
          curl -fsSL "https://raw.githubusercontent.com/bharath2020/steward/$GITHUB_SHA/install.sh" | bash -s -- --no-open
          cd "$STEWARD_INSTALL_DIR"
          node -e 'if(require("fs").readdirSync("runtime/runs").length!==1)process.exit(1)'
      - name: Run repository verification
        working-directory: ${{ runner.temp }}/Steward
        run: npm run verify
      - name: Upload setup evidence
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: isolated-macos-setup-${{ github.sha }}
          path: |
            ${{ runner.temp }}/Steward/output/isolated-setup-verdict.json
            ${{ runner.temp }}/Steward/runtime/services/*.log
            ${{ runner.temp }}/Steward/runtime/services/setup-start.json
            ${{ runner.temp }}/Steward/runtime/runs/
          if-no-files-found: warn
          retention-days: 7

  verify:
    name: Verify (Node ${{ matrix.node }})
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        node: [22, 24]
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0
        with:
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: ${{ matrix.node }}
          cache: npm
      - name: Install locked dependencies
        run: npm ci
      - name: Typecheck and test
        run: npm run verify

  recovery:
    name: Simulated recovery (Node 24)
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0
        with:
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          cache: npm
      - name: Install locked dependencies
        run: npm ci
      - name: Install Temporal CLI 1.8.2
        shell: bash
        working-directory: ${{ runner.temp }}
        run: |
          curl --fail --silent --show-error --location --retry 3 \
            --output temporal_cli_1.8.2_linux_amd64.tar.gz \
            https://github.com/temporalio/cli/releases/download/v1.8.2/temporal_cli_1.8.2_linux_amd64.tar.gz
          printf '%s\n' 'd8421bda989e6514b4bdb4d63a9012a8a05a806892e881a5aad8510496349a94  temporal_cli_1.8.2_linux_amd64.tar.gz' | sha256sum --check --strict
          mkdir -p temporal-bin
          tar -xzf temporal_cli_1.8.2_linux_amd64.tar.gz -C temporal-bin temporal
          echo "$RUNNER_TEMP/temporal-bin" >> "$GITHUB_PATH"
      - name: Verify installed Temporal version
        run: temporal --version
      - name: Verify worker-loss and session recovery
        # The harness owns its temp directory, server ports, and simulated runs.
        # It does not need Codex credentials or the repository runtime directory.
        run: npm run verify:recovery
