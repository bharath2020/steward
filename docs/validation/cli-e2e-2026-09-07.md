# CLI end-to-end validation — 2026-09-07

User outcome: exercise the implemented consumer CLI and every shipped YAML example through real subprocesses and an isolated durable runtime. See [coverage and limits](../cli-testing.md).

Validation on macOS with Node 22.23.2:

| Command | Result |
| --- | --- |
| `npm run verify` | Exit 0; typecheck and 70 existing tests passed |
| `npm run build` after the final test additions | Exit 0 |
| `npm run test:e2e` | Exit 0; 46 scenario subtests plus parent test passed; 28.9 seconds |
| `npm run verify:recovery` | Exit 0; all five existing recovery/failure scenarios passed |

The successful CLI matrix retained its database, generated fixtures, logs, snapshots, transitions and agent receipts at `/private/var/folders/1c/wkzr1t1945v0tg_882wwqshh0000gn/T/steward-cli-e2e-fhikzi`. This is a local temporary evidence location, not a portable release artifact. CI uploads the equivalent directory from its own run.

Initial sandboxed attempts failed on loopback/tsx IPC `listen EPERM`; validation above used authorized local service permissions. Early harness assertions incorrectly expected human and aggregate queue output files, and selected the first loop iteration's completion event. The final assertions follow the existing storage contract and use the last loop completion event. No production source behavior was changed to obtain a pass.

Recovery output reported zero provider reruns after worker loss, no rerun of completed siblings, two independently recoverable queue items, retained corrupt dispatch evidence with a fresh recovery cycle, and Temporal `FAILED` for an invalid array source.

CI configuration was updated but its remote execution and clean-host macOS installation were not run in this local session. Simulated-provider validation does not establish live Codex behavior or any production release gate.
